<?php

// If the data parameter does not include a position key, then append. If position is included, 0 for the beginning and any other integer to insert in between parts (if the provided integer is higher than the number of parts, append at the end withoutthrowing an error. This reduces overhead on the user's part and improves overall developer experience) 

function insert($episode_id, $part_data) {
  $part_id = generate_id();
}

// The 
function delete_by_id($episode_id, $part_id) {
}

function delete_by_position($episode_id, $position) {
}

function sort($episode_id, $sorted_parts) {
}

